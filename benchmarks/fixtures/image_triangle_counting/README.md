# Image counting fixtures

Save the Player Status benchmark source image as:

`player_status_v1.png`

Keep the original dimensions and encoding where possible. Do not crop, resize,
recompress, annotate, or rename it: the V1 region manifest uses normalized
coordinates, but retaining the original bytes makes results reproducible and
keeps provenance honest.

The corresponding expectations and crop boundaries are in
`../../image_triangle_counting_player_status_v1.json`.
